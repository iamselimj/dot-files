# NVIM Commands

## List of commands = 0 to LSP @Primeagen
nvim . = open the directory
:h rtp = list folders where nvim lives
% = create a new file
d = create a new directory

after editing a file (save and go back to explorer) macro to create
:w
:Ex
